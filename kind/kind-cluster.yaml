# Kind cluster configuration for the Production-Grade GitOps Platform.
# This configuration creates a multi-node Kubernetes cluster consisting of:
# - 1 Control Plane Node
# - 2 Worker Nodes

kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane

  - role: worker

  - role: worker