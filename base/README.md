# base

This directory will hold common Kubernetes resource definitions used by all environments.

Future contents will include:

- shared deployments, services, and config maps
- reusable Kustomize resources and common patches
- base manifests that are extended by environment overlays
