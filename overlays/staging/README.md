# overlays/staging

This directory will contain the staging environment overlay.

Future contents will include:

- Kustomize patches and customizations for staging
- environment-specific values, resource limits, and namespace definitions
- pre-production deployment configuration and validation settings
