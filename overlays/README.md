# overlays

This directory will contain Kustomize overlays for environment-specific customization.

Future contents will include:

- shared overlay configuration for development, staging, and production
- environment selectors, common patches, and variant definitions
- higher-level Kustomization manifests to organize overlays
